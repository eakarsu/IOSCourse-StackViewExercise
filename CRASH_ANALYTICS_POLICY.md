# Crash and analytics policy

Crash reporting and analytics are disabled by default. Enabling either requires a reviewed provider contract, opt-in configuration, data minimization, retention limits, deletion handling, a privacy-policy update, and an App Privacy disclosure. Never attach item titles, details, endpoint URLs, tokens, or cached payloads to telemetry.
